# Secrets — NEVER commit
config.json
*.key
*.pem
.env

# Working files (regenerated per run) — only data/YYYY-MM-DD.md + data/index.json are versioned
data/raw-*.json
data/majorstock-*.json
data/docs/

# Logs / OS
*.log
.DS_Store
Thumbs.db
desktop.ini

# Local dev config
.claude/

# Thesis 보드: DART 원문 캐시(빌드 입력, 재생성 가능) — thesis-candidates.json만 런타임 필요
data/doccache/

# Funding screener intermediates (regenerated by funding\build.ps1)
# versioned: data/funding-pool.json + data/funding-pool.csv (dashboard needs them)
data/funding-panel.json
data/corpcode.json
data/funding-events.json